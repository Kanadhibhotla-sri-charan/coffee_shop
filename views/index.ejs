<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Coffee Shop</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>Welcome to Coffee Shop</h1>

    <% if (successMessage) { %>
        <div class="alert alert-success"><%= successMessage %></div>
    <% } %>
    
    <% if (errorMessage) { %>
        <div class="alert alert-danger"><%= errorMessage %></div>
    <% } %>
    
    

    <ul>
        <% if (coffees.length === 0) { %>
            <div style="color: red; font-weight: bold; text-align: center; margin-top: 20px;">
                Sorry! Out of stock currently. We will serve you very soon.
            </div>
        <% } else { %>
            <% coffees.forEach(coffee => { %>
            <% }); %>
        <% } %>
        
        <% coffees.forEach(coffee => { %>
            <li>
                <strong><%= coffee.name %></strong> - ₹<%= coffee.price %> 
                (Available: <%= coffee.quantity %>)
                <form action="/order" method="POST">
                    <input type="hidden" name="coffee_id" value="<%= coffee.coffee_id %>">
                    <input type="number" name="quantity" min="1" max="<%= coffee.quantity %>" required
                    oninvalid="this.setCustomValidity('We currently have only <%= coffee.quantity %> cup(s) available. Please enter a valid quantity.')"
                    oninput="this.setCustomValidity('')">
                    <button type="submit">Order</button>
                </form>
            </li>
        <% }); %>   
    </ul>
</body>

</html>
